module DecisionTree
  class AccuracySplitter
    def self.choose_attribute(dataset)
      #How to do this? Split, check the accuracy of your split dataset
      #Split the dataset on an attribute
      #Check the accuracy of the dataset when split on that attribute
      #
      accuracies = []
      
    end
  end
end
